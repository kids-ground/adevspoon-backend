spring:
  profiles:
    default: local
    group:
      local:
        - infrastructure
        - domain-local
#      dev:
#        - infrastructure-dev
#        - domain-dev
#        - logging
#        - metrics
#      prod:
#        - infrastructure-prod
#        - domain-prod
#        - common-prod
#        - metrics

---
spring:
  config:
    activate:
      on-profile: local, dev
  docker:
    compose:
      lifecycle-management: start_only
      file: ./adevspoon-api/docker-compose-local.yml
      enabled: true